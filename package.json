{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ui_dev": "lerna run storybook",
    "mobile_dev": "lerna run dev",
    "legacy_dev": "lerna run books_start"
  }
}
